// Root build.gradle

ext {
    set('springBootVersion', '3.2.5')
    set('springDependencyManagementVersion', '1.1.4')
    set('javaVersion', JavaVersion.VERSION_17)
}

// just for note
allprojects {
    // No plugins, no repositories, no dependencies
}
